# Room, Compose, and kotlinx.serialization all ship their own consumer rules;
# nothing app-specific needed yet. flasher-native's JNI callback methods are
# covered by flasher-native/consumer-rules.pro (applied automatically).
